# 1.1 透明穿透与拖动柄技术总结

## 本次目标

桌面宠物窗口采用透明、无边框设计后，需要同时满足两项体验：

1. 鼠标移到角色上时，三横线拖动柄延迟、平滑地显示出来。
2. 鼠标落在角色 PNG 的透明区域或窗口空白区域时，事件能穿透到桌面和下方应用；落在角色不透明像素上时，角色仍可以点击和拖动。

这两个目标分别涉及网页动画与 Tauri 原生窗口事件，不能只用 CSS 完成。

## 一、拖动柄的延迟显示

### 视觉状态

拖动柄默认处于隐藏状态：

```css
opacity: 0;
visibility: hidden;
transform: translateY(-0.4rem) scale(0.92);
```

- `opacity` 负责透明度，从 `0` 过渡到 `1`。
- `transform` 让图标从略微上方、略小的状态移动到正常位置和大小。
- `visibility: hidden` 让尚未出现的图标无法被鼠标误点。

当程序判定鼠标正在角色或拖动柄区域时，会为组件增加 `pet-avatar--interactive` class。此时拖动柄等待 `180ms` 后显示，并使用缓入缓出的曲线完成动画：

```css
transition:
  opacity 160ms ease,
  transform 180ms cubic-bezier(0.22, 1, 0.36, 1),
  visibility 0s linear 160ms;

transition-delay: 180ms, 180ms, 180ms;
```

其中 `cubic-bezier(0.22, 1, 0.36, 1)` 会产生较柔和的减速停靠感；鼠标离开时没有额外延迟，因此图标会立即开始淡出。

## 二、为什么 `background: transparent` 不够

以下 CSS 只影响视觉：

```css
body {
  background: transparent;
}
```

它会让窗口背景看起来透明，但原生窗口仍覆盖在桌面上，仍会截获鼠标事件。因此“看得到桌面”不代表“点得到桌面”。

真正的鼠标穿透由 Tauri 窗口 API 完成：

```ts
await getCurrentWindow().setIgnoreCursorEvents(true)
```

传入 `true` 后，操作系统会把鼠标事件交给该窗口下方的内容；传入 `false` 后，宠物窗口恢复接收鼠标事件。

## 三、像素级命中检测流程

PNG 本身包含透明像素。为了不把角色周围的透明区域也当成可点击区域，`PetAvatar.vue` 会按以下流程判断鼠标位置。

```text
角色 PNG 加载完成
        ↓
绘制到离屏 Canvas
        ↓
读取并缓存每个像素的 Alpha 值
        ↓
每 80ms 获取系统鼠标位置与窗口位置
        ↓
换算为 Vue 页面中的坐标
        ↓
检查该位置的 PNG Alpha 值
        ↓
不透明像素 / 拖动柄 → 窗口接收鼠标
透明像素 / 空白背景 → 窗口忽略鼠标，事件穿透
```

### 1. 缓存 Alpha 通道

图片加载完成后，代码把图片绘制到不显示在页面上的 Canvas，再读取 `ImageData`。每个像素由四个数值组成：`R`、`G`、`B`、`A`；第四个值 `A` 就是 Alpha 透明度。

```ts
alphaPixels = context.getImageData(0, 0, canvas.width, canvas.height).data
```

本项目将 Alpha 大于 `12` 的像素视为角色实体。小阈值可以保留头发、衣角等半透明的抗锯齿边缘，同时避免几乎完全透明的像素阻挡鼠标。

### 2. 处理 `object-fit: contain`

角色图片使用 `object-fit: contain` 保持原图比例。图片元素的矩形区域不一定等于角色实际绘制区域，因为元素可能包含留白。

因此程序会计算缩放比例、实际渲染宽高与居中偏移量，再把页面坐标转换回原图像素坐标，最后读取对应像素的 Alpha 值。这样窗口穿透判定与屏幕上看到的角色轮廓一致，而不是简单使用一整个矩形点击区。

### 3. 控制原生窗口的事件模式

`updateCursorEventMode()` 每 80ms 执行一次。它会调用：

- `cursorPosition()`：读取桌面中的鼠标物理坐标。
- `innerPosition()`：读取窗口内容区的物理坐标。
- `scaleFactor()`：处理高 DPI 显示器的物理像素与 CSS 像素换算。
- `setIgnoreCursorEvents()`：在“接收事件”和“穿透事件”之间切换。

只有状态变化时才调用 `setIgnoreCursorEvents()`，避免每次轮询都重复请求原生窗口。

## 四、拖动与穿透如何共存

拖动柄按下鼠标时调用：

```ts
await getCurrentWindow().startDragging()
```

拖动期间，代码会暂时保持窗口接收鼠标事件，防止鼠标移动到透明部分时提前进入穿透模式而中断拖动。收到 `mouseup` 后，再恢复常规的像素命中判断。

因此当前交互顺序是：

```text
鼠标进入角色不透明区域
        ↓
窗口恢复接收事件
        ↓
等待 180ms，三横线平滑显示
        ↓
按住三横线，调用 startDragging()
        ↓
松开鼠标，恢复透明像素穿透判定
```

## 五、Tauri capability 权限

Tauri 2 会通过 capability 控制前端调用原生能力。`src-tauri/capabilities/default.json` 中本次需要两项明确权限：

```json
"core:window:allow-start-dragging",
"core:window:allow-set-ignore-cursor-events"
```

前者允许开始原生窗口拖动，后者允许切换鼠标事件穿透。修改 capability 后必须关闭旧窗口并重新执行 `npx tauri dev`；Vite 的热更新无法重新加载 Rust/Tauri 端权限。

## 六、当前限制与下一步

- 鼠标位置每 80ms 检查一次，因此重新进入角色可交互区域可能存在不超过约 80ms 的感知延迟。
- 拖动柄除轮询延迟外，还有刻意设计的 180ms 显示延迟。
- 当前像素命中逻辑针对单张 PNG 角色。未来接入 Live2D 时，需要改为根据 Live2D 模型的可交互区域判断，而不是读取 PNG 的 Alpha 通道。

## 相关文件

- `src/components/PetAvatar.vue`：动画、Alpha 命中检测、拖动和穿透控制。
- `src-tauri/capabilities/default.json`：Tauri 原生窗口权限。
- `src/style.css`：页面透明背景。
