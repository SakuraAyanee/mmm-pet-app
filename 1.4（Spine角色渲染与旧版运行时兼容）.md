# 1.4 Spine 角色渲染与旧版运行时兼容

## 本次目标

把一组 Spine 角色资源接入 Vue 3 + TypeScript 项目，先建立独立的 `SpineAvatar.vue` 渲染组件，验证模型可以加载并循环播放 `wait` 动画。本阶段只验证渲染链路，尚未把原有静态宠物的窗口拖动、像素穿透、右键菜单和缩放交互迁移到 Canvas。

本次资源位于：

```text
public/spine/mamimi/
├── data.json
├── data.atlas
└── data.png
```

## 一、资源检查结果

`data.json` 可以正常解析，模型基本信息如下：

- Spine 导出版本：`3.6.53`
- 模型记录尺寸：约 `422.47 × 972.75`
- 骨骼数量：79
- 插槽数量：59
- 皮肤：`default`
- 动画数量：48
- 事件：`loop_start`、`relay`

`data.png` 是有效的 `1024 × 1024` RGBA PNG，包含透明通道；`data.atlas` 定义约 64 个纹理区域，并且正确引用 `data.png`。

具有代表性的动画包括：

```text
待机：wait, wait1, wait2, wait3, wait4
交互：hello, touch, play, yes, no, think, sleep
情绪：anger1, sad1, shy1, smile1, surp1 等
表情：face_smile, face_sad, face_anger, face_cry 等
口型：lip_a, lip_smile, lip_sad, lip_wait 等
```

## 二、Spine 三类文件如何协作

Spine 并不是播放一张完整人物图片，而是在每一帧根据骨骼数据移动许多局部图片：

```text
data.json  → 骨骼、插槽、网格、皮肤、动画关键帧
     ↓
data.atlas → 每个身体部件在纹理图中的区域和旋转方式
     ↓
data.png   → 头发、面部、身体、服装等实际像素
```

组件只主动加载 `data.json`。pixi-spine 根据同名规则找到 `data.atlas`，再读取 Atlas 第一行引用的 `data.png`。所以文件之间的名称不能随意修改；如果必须改名，也要同步修改引用。

## 三、为什么采用 PixiJS 4 与 pixi-spine 1.4

Spine Runtime 对版本非常敏感。模型由 Spine `3.6.53` 导出，因此要使用兼容 Spine 3.6 的运行时，而不能直接使用面向 Spine 3.8、4.1 或 4.2 的最新版。

本项目固定安装：

```json
{
  "pixi.js": "4.8.9",
  "pixi-spine": "1.4.0"
}
```

这里不使用 `^`，是为了防止重新安装依赖时自动升级到不兼容的版本。Spine 官方的版本原则是：导出数据与 Runtime 的主版本、次版本必须一致；补丁版本可以不同。

本方案使用的是历史运行时，应注意：

- 旧依赖的维护和现代工具链兼容性较弱；
- 将来升级 Runtime 时通常也要重新导出模型；
- 构建产物会明显增大；
- Spine Runtime 和角色素材都有各自的许可要求，公开发布前必须确认授权。

## 四、Vue、PixiJS 与 Spine Runtime 的职责

三个部分的职责可以理解为：

```text
Vue           = 管理组件的创建、属性和销毁
PixiJS        = 创建透明 WebGL Canvas 并持续绘制画面
Spine Runtime = 解析骨骼数据并计算每一帧的姿势
```

完整运行顺序是：

```text
App.vue 创建 SpineAvatar
        ↓
Vue 完成 DOM 挂载并触发 onMounted
        ↓
创建 PixiJS Application 和透明 Canvas
        ↓
Loader 请求 data.json、data.atlas、data.png
        ↓
Spine Runtime 组装 SkeletonData
        ↓
创建 Spine 显示对象并放入 PixiJS Stage
        ↓
播放 wait 动画
        ↓
PixiJS 每帧把骨骼计算结果绘制到 Canvas
```

## 五、独立组件的 Props

`SpineAvatar.vue` 接收两个属性：

```ts
defineProps<{
  animation?: string
  loop?: boolean
}>()
```

默认值为：

```ts
animation: 'wait'
loop: true
```

父组件可以这样使用：

```vue
<SpineAvatar animation="wait" />
<SpineAvatar animation="hello" :loop="false" />
```

第二个例子中，属性前面的 `:` 表示传递 JavaScript 布尔值 `false`，而不是字符串 `"false"`。

## 六、ref 与 onMounted 的作用

模板准备了 Canvas 容器：

```vue
<div ref="canvasHost" class="spine-avatar__canvas-host"></div>
```

脚本通过 Vue 的 `ref` 保存真实 DOM 元素：

```ts
const canvasHost = ref<HTMLDivElement | null>(null)
```

组件挂载前，`canvasHost.value` 是 `null`；挂载完成后，它才指向真实的 `<div>`。因此渲染器初始化放在 `onMounted` 中：

```ts
onMounted(() => {
  void createRenderer()
})
```

这体现了 Vue 组件的基本生命周期：先建立响应式状态和模板，再挂载 DOM，最后初始化依赖真实 DOM 的第三方库。

## 七、模型加载与舞台树

模型入口地址使用 Vite 的基础路径生成：

```ts
const modelUrl = `${import.meta.env.BASE_URL}spine/mamimi/data.json`
```

加载并解析成功后创建角色：

```ts
avatar = new PIXI.spine.Spine(resource.spineData)
app.stage.addChild(avatar)
```

这里形成一棵简单的显示树：

```text
PixiJS Stage
└── Spine Avatar
    ├── Skeleton
    ├── Slots
    └── Mesh / Sprite attachments
```

只有加入 `stage` 的对象才会被 PixiJS 渲染。

## 八、动画播放接口

组件封装了统一的动画播放函数：

```ts
function playAnimation(name: string, loop = true) {
  if (!avatar || !avatar.state.hasAnimation(name)) {
    return false
  }

  avatar.state.setAnimation(0, name, loop)
  return true
}
```

`setAnimation(0, name, loop)` 的三个参数分别是：

1. 动画轨道编号，`0` 是基础轨道；
2. 模型中的动画名称；
3. 是否循环播放。

播放前用 `hasAnimation()` 检查名称，可以把拼写错误从“角色静止但没有说明”转化为明确的组件错误状态。

组件还通过：

```ts
defineExpose({ playAnimation })
```

把该函数暴露给父组件。后续可以让右键菜单、单击或定时器调用 `touch`、`hello`、`sleep` 等动作。

## 九、角色自适应缩放和定位

模型加载后，代码通过：

```ts
const bounds = avatar.getLocalBounds()
```

取得角色在自身坐标系中的实际边界，然后分别计算容器宽度和高度能容纳的缩放比例：

```ts
const scale = Math.min(
  availableWidth / bounds.width,
  availableHeight / bounds.height,
)
```

使用 `Math.min()` 可以保证宽度和高度都不会溢出。例如宽度允许放大到 `1.2`，高度只允许 `0.8`，最终必须使用 `0.8`。

定位时让角色水平居中，并让实际边界的底部落在容器底部预留位置，而不是假设 Spine 原点就是脚底：

```ts
avatar.position.set(
  width / 2 - (bounds.x + bounds.width / 2) * scale,
  height - padding - (bounds.y + bounds.height) * scale,
)
```

`ResizeObserver` 监听容器尺寸变化。当浏览器窗口或后续宠物尺寸发生变化时，只重新计算渲染器尺寸与角色位置，不重新下载模型。

## 十、加载状态和错误处理

组件维护三种状态：

```ts
const status = ref<'loading' | 'ready' | 'error'>('loading')
```

- `loading`：显示“模型加载中”；
- `ready`：隐藏提示，显示角色；
- `error`：显示具体加载或动画名称错误。

这比空白 Canvas 更容易排查问题。失败可能来自文件路径错误、Atlas 与 PNG 不匹配、Spine 版本不兼容或动画名称不存在。

## 十一、旧库与现代 TypeScript 的兼容层

### 1. pixi-spine 不是现代 ESM 模块

pixi-spine 1.4 会从全局查找 `PIXI`，因此必须先暴露 PixiJS，再动态加载插件：

```ts
;(globalThis as typeof globalThis & { PIXI: typeof PIXI }).PIXI = PIXI
await import('pixi-spine')
```

动态 `import()` 很重要：普通静态 `import` 会被提升并提前执行，此时全局 `PIXI` 可能尚未建立。

### 2. 缺少模块类型声明

旧包没有符合现代模块解析方式的声明，所以增加：

```ts
declare module 'pixi-spine'
```

它只告诉 TypeScript 该模块存在，不会生成运行时代码。

### 3. 旧声明缺少 PIXI.Application

运行时实际存在 `PIXI.Application`，但包内附带的旧类型声明没有完整描述它。本项目用局部的 `LegacyPixiApplication` 接口补充需要的最小类型范围，没有关闭整个项目的 TypeScript 类型检查。

## 十二、Vite 8 中的 global 错误

第一次生产构建虽然成功，但浏览器运行时出现：

```text
ReferenceError: global is not defined
```

原因是 PixiJS 4 的部分代码假设存在 Node 风格的 `global`，而现代浏览器和 Vite 8 使用标准的 `globalThis`，不会再自动注入旧变量。

最终在 `vite.config.ts` 中加入：

```ts
define: {
  global: 'globalThis',
}
```

这让构建器把旧库使用的 `global` 映射为浏览器标准对象。修复后，模型在实际浏览器中成功显示并播放。

## 十三、组件卸载与资源释放

开发环境热更新或父组件切换时，Vue 会卸载旧组件。此时要主动清理第三方资源：

```ts
onUnmounted(() => {
  resizeObserver?.disconnect()
  loader?.reset()
  app?.destroy(true)
})
```

清理内容包括：

- 停止尺寸观察；
- 停止或重置资源加载器；
- 销毁 PixiJS 渲染器和 Canvas；
- 清除对象引用。

如果忽略清理，热更新或反复切换组件后可能出现多个 Canvas、重复动画循环以及显存泄漏。

## 十四、验证结果

本次进行了两类验证：

1. `npm run build`：Vue TypeScript 检查和 Vite 生产构建通过；
2. 浏览器实际运行：模型、纹理和 `wait` 待机动画正常显示。

Vite 会对 PixiJS 4 中引用的 Node `path`、`url` 模块给出浏览器外部化警告。目前使用到的 Spine 渲染链路可以正常运行，但这些警告也说明旧依赖存在技术债务，后续升级或打包 Tauri 安装程序时仍需回归测试。

## 十五、当前边界与下一步

目前 `App.vue` 暂时直接显示 `SpineAvatar`，原有 `PetAvatar.vue` 没有删除。下一阶段应当保持职责分离：

```text
PetAvatar.vue
├── 窗口拖动
├── 右键菜单
├── 缩放与偏好保存
├── 透明区域穿透判断
└── SpineAvatar.vue
    └── 只负责模型加载、布局和动画播放
```

建议按以下顺序继续：

1. 将 `SpineAvatar` 嵌回 `PetAvatar`，替换静态 `<img>`；
2. 把单击跳跃替换为 Spine 的 `touch` 等交互动画；
3. 动画播放结束后自动回到 `wait`；
4. 为 Canvas 重做透明像素命中检测；
5. 恢复拖动、菜单、缩放和窗口穿透；
6. 在 Tauri WebView 中验证 GPU 渲染、DPI、透明窗口和资源路径；
7. 公开仓库或发布安装包前确认 Spine Runtime 与角色素材授权。

## 本阶段值得记忆的知识点

- Spine、Live2D 和普通 GIF/视频是不同的技术体系；
- Spine 的 JSON、Atlas、PNG 缺一不可；
- 动画数据版本必须与 Runtime 主、次版本匹配；
- Vue 的 `onMounted` 适合初始化依赖真实 DOM 的第三方库；
- `onUnmounted` 必须释放 Canvas、监听器和渲染器；
- PixiJS 的 Stage 是一棵显示对象树；
- Spine 动画通过轨道进行播放和混合；
- `ResizeObserver` 用于响应元素尺寸变化；
- 旧库接入现代工具链时，应建立小而明确的兼容层，而不是关闭类型检查；
- “构建成功”不等于“运行成功”，Canvas/WebGL 功能必须做实际页面验证。
