# 1.2 角色交互与原生拖放冲突修复

## 本次调整

在接入 Live2D 前，先将静态 PNG 宠物的交互收敛为更接近桌面宠物的模式：

1. 删除心情、颜色变化、名称和点击次数等临时演示状态。
2. 轻点角色时播放跳跃动画，不再随机移动角色。
3. 拖动角色时移动桌面窗口，不必只能拖动三横线图标。
4. 禁止浏览器把角色 PNG 当成文件拖走，避免出现重影、保存或复制图片到桌面的现象。

## 一、点击与拖动的阈值判断

同一个角色区域既要支持“点击跳跃”，也要支持“拖动窗口”。如果鼠标一按下就开始拖动，用户很难完成点击；如果只监听点击，则不能自然地拖动角色。

因此采用 **拖动阈值（drag threshold）** 判断手势。

```text
指针按下角色
      ↓
记录按下时的坐标
      ↓
指针移动
      ↓
移动距离 < 6px ──→ 松开时视为点击，播放跳跃动画
      ↓
移动距离 ≥ 6px ──→ 视为拖动，调用 Tauri startDragging()
```

代码使用 `PointerEvent`，因为它能统一处理鼠标、触控笔和触摸设备的按下、移动、抬起等事件。

```ts
const distance = Math.hypot(
  event.clientX - petPointerStart.x,
  event.clientY - petPointerStart.y,
)
```

`Math.hypot(x, y)` 根据横向和纵向位移计算直线距离。`6px` 是一个较小的容错范围：能过滤正常点击时的手部微小抖动，又不会让拖动显得迟钝。

`setPointerCapture()` 会在按住角色后继续把后续指针事件交给角色按钮，即使光标暂时移出按钮范围，也不会丢失拖动判断。

## 二、点击跳跃动画

点击被判定成立后，Vue 将 `isJumping` 设为 `true`，进而为角色按钮添加 `pet-avatar__body--jumping` class。CSS 根据这个 class 运行 `pet-jump` 关键帧：

```text
原位
  ↓
向上跳 1.5rem，并轻微放大
  ↓
落地时略微缩小
  ↓
回到正常尺寸
```

动画持续 `480ms`。动画结束事件 `animationend` 会将 `isJumping` 还原为 `false`。再次点击前，代码会先移除 class、再通过 `requestAnimationFrame()` 在下一帧重新添加，因此连续点击也可以从头播放动画。

## 三、三横线拖动柄的延迟淡出

三横线不是唯一拖动入口，而是可发现性提示：新用户能一眼理解窗口可拖动，熟悉后可直接拖动角色。

- 指针进入角色后，拖动柄等待 `180ms` 再平滑显示。
- 指针离开角色后，显示状态额外保留 `420ms`。
- 保留时间结束后，图标依据 CSS 的 `opacity`、`visibility` 与 `transform` 过渡淡出。
- 在保留期间重新进入角色或图标，会取消隐藏计时器。

这种方式比立刻隐藏更稳定，不会因鼠标从角色移动到拖动柄时出现明显闪烁。

## 四、图片为何会被“抓取”到桌面

### 现象

长按角色并移动时，图片附近出现重影；松开鼠标后，桌面可能收到图片保存、复制或投放操作。

### 原因：HTML5 Drag and Drop

浏览器和 WebView 中的 `<img>` 默认支持原生 HTML5 拖放（Drag and Drop）。当用户拖动图片时，浏览器会生成一个拖拽预览，通常称为 **拖拽预览图** 或 **幽灵图（drag ghost image）**。

此时项目自己的 Tauri 窗口拖动和浏览器的图片拖放会同时竞争同一个手势。若鼠标在桌面释放，操作系统可能将其理解为把图片投放到桌面。

## 五、修复策略

修复目标是：保留由项目控制的 Tauri 窗口拖动，禁止浏览器控制的图片拖放。

### 1. 关闭图片的可拖放属性

```vue
<img draggable="false" />
```

`draggable="false"` 是最直接的声明，表示该图片不能作为 HTML5 拖放源。

### 2. 阻止 `dragstart` 事件

```vue
@dragstart.prevent
```

这个事件同时加在角色按钮和内部图片上，作为额外保护：即使浏览器仍尝试开始拖拽，Vue 也会通过 `preventDefault()` 取消它。

### 3. 禁止选中与 WebKit 图片拖拽

```css
user-select: none;
-webkit-user-select: none;
-webkit-user-drag: none;
```

- `user-select: none`：避免拖动时选中文本或图片内容。
- `-webkit-user-select: none`：兼容 Chromium/WebView 的前缀版本。
- `-webkit-user-drag: none`：进一步关闭 WebKit/Chromium 的图片拖拽行为。

## 六、事件职责划分

```text
PointerEvent
  ├─ 位移不足 6px → jumpPet() → CSS 跳跃动画
  └─ 位移达到 6px → startDragging() → Tauri 原生窗口拖动

HTML Drag and Drop
  └─ dragstart → preventDefault() → 不生成幽灵图，不投放图片
```

关键结论：窗口拖动由 Tauri 的 `startDragging()` 负责；图片的 HTML5 拖放必须明确禁用。两者不要混用，否则会产生幽灵图、拖放到桌面等非预期行为。

## 相关文件

- `src/components/PetAvatar.vue`：角色手势、跳跃动画、拖动柄显示与原生拖放拦截。
- `src/types/pet.ts`：旧的 `PetState` 演示类型，已随心情/计数功能删除。
