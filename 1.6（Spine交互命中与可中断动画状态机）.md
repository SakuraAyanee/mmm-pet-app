# 1.6 Spine 交互命中与可中断动画状态机

## 本阶段目标

1. 让桌宠窗口可以选择是否总在其他应用上方；
2. 改善缩放模式的操作直觉，使角色区域仍可用于拖动窗口；
3. 用 Spine 默认姿势生成命中蒙版，移除对旧透明 PNG 的依赖；
4. 建立可拖动的动画测试工作台，验证模型中已有的动作组合；
5. 修复 Spine 裁剪兼容问题，并保留高清渲染与缩放能力；
6. 支持作者提供的局部表情动画，同时默认尊重动作原有表情；
7. 使用可中断动画状态机处理连续点击，避免跳过复位动作后出现骨骼错位。

本阶段把桌宠交互进一步分成三层：

```text
PetAvatar.vue
├── Tauri 窗口能力与右键菜单
├── 指针命中、窗口穿透和拖动
├── 缩放编辑与本地持久化
├── 动画测试工作台
└── 向 SpineAvatar 提交播放请求

SpineAvatar.vue
├── PixiJS / pixi-spine 渲染
├── 默认姿势 Alpha 蒙版采集
├── Spine 事件元数据解析
├── 动作、表情与复位轨道
└── 可中断动画状态机

data.json
├── 动画关键帧
├── loop_start 循环段标记
└── relay 复位动画声明
```

## 一、窗口总在最前

右键菜单新增“总在最前/取消总在最前”。Vue 通过 Tauri Window API 调用：

```ts
await appWindow.setAlwaysOnTop(enabled)
```

Tauri 2 会按 Capability 检查前端是否有权调用原生窗口命令，因此同时在：

```text
src-tauri/capabilities/default.json
```

加入：

```json
"core:window:allow-set-always-on-top"
```

用户选择保存在：

```text
localStorage['mmm-pet-always-on-top']
```

应用启动后读取并恢复设置。默认值为开启，调用失败时会捕获异常，避免菜单操作导致整个 Vue 组件中断。

## 二、缩放模式也允许拖动窗口

旧逻辑在 `isResizing` 为 `true` 时直接禁止角色拖动。虽然能够防止手势冲突，但用户看到整个人物和调整框后，会自然地认为人物本身也能拖动。

本阶段取消这项限制：

```text
角色按下并移动超过阈值 → 拖动原生窗口
右下角缩放柄按下并移动 → 修改 petScale
单击角色                 → 仅在非缩放模式播放点击动画
```

缩放柄通过独立事件和指针捕获处理，因此不会被外层角色拖动手势接管。这样窗口移动与尺寸调整可以共存，同时避免调整大小时误触发动画。

## 三、从 Spine Canvas 生成默认命中蒙版

### 1. 为什么移除旧 PNG 蒙版

接入 Spine 后，用户看到的是 Canvas 中的骨骼动画，而旧命中区域仍来自静态人物 PNG。两者的构图、边缘和缩放可能不同，从而出现：

- 鼠标停在可见角色上却发生穿透；
- 鼠标停在透明区域却仍被窗口捕获；
- 更换角色时还需要人工准备另一张透明图。

### 2. 当前方案

模型加载完成并播放默认 `wait` 后，`SpineAvatar` 使用 Pixi Renderer 的 `extract.canvas()` 取得一次渲染快照，再读取每个像素的 Alpha：

```text
Spine 默认 wait 姿势
→ WebGL Canvas 渲染
→ renderer.extract.canvas()
→ getImageData()
→ 提取 Alpha 通道
→ emit('hitMaskReady')
→ PetAvatar 保存蒙版
```

蒙版只保存一份 `Uint8Array`：

```ts
interface SpineHitMask {
  alpha: Uint8Array
  width: number
  height: number
}
```

指针坐标先换算到角色容器的相对比例，再映射到蒙版像素。角色放大或缩小时只改变映射比例，不重新截图，因此缩放操作不会反复触发 WebGL 像素读取。

Alpha 大于阈值 `12` 时认为指针落在角色上，否则调用：

```ts
appWindow.setIgnoreCursorEvents(true)
```

菜单、拖动柄、缩放按钮和动画工作台会被单独加入交互白名单，防止这些控件位于透明区域时被错误穿透。

### 3. 方案边界

当前采集的是默认 `wait` 姿势，而不是每帧动态蒙版。它能解决默认角色轮廓与旧 PNG 不一致的问题，也避免频繁读取 GPU 像素，但大幅度动作伸出的手臂仍可能超出默认命中范围。

后续可考虑低频动态采集、骨骼包围盒或简化多边形命中区域，但需要评估 CPU/GPU 同步成本和交互稳定性。

## 四、Spine 裁剪兼容修复

模型数据由 Spine 3.6 导出，并使用了裁剪相关数据。原先安装的 `pixi-spine 1.4.0` 对这类数据的支持不完整，表现为眼珠等附件越过眉眼区域显示，看起来像是图层顺序错误。

实际问题不是简单的 CSS `z-index`，因为 Spine 插槽顺序、蒙版和裁剪都发生在同一个 WebGL Canvas 内。外部 CSS 无法改变骨骼内部的绘制关系。

本阶段将运行时升级为：

```json
"pixi-spine": "1.5.16",
"pixi.js": "4.8.9"
```

`pixi-spine 1.5.16` 仍与项目使用的 PixiJS 4 配套，同时补充了 Spine 3.6 和 clipping 支持。升级后眼睛越界问题明显改善，又避免直接迁移到新版 PixiJS 所带来的大范围 API 重写。

## 五、动画测试工作台

右键菜单新增“点击动画测试”。工作台可以拖动，避免固定面板遮挡人物，并在窗口边缘进行位置限制。

工作台包含：

- 动作预设选择；
- 当前预设的技术说明；
- 可选表情覆盖；
- “预览”按钮；
- “设为点击动画”按钮。

保存后的动作与表情分别写入：

```text
localStorage['mmm-pet-click-animation']
localStorage['mmm-pet-click-expression']
```

旧版本保存的 `shy-response` 和 `full-touch` 会迁移到新的预设 ID，避免升级后用户设置无故丢失。

## 六、从模型事件恢复作者设计的动作链

模型动画中存在两类关键事件：

```text
loop_start → 前置动作结束、可重复片段开始的位置
relay      → 主动作之后应播放的收手或复位动画
```

测试工作台加入了所有能够从模型中明确确认的组合：

| 主动画 | 作者指定的 relay | 最终状态 |
| --- | --- | --- |
| `anger1` | `arm_down_R` | `wait` |
| `hello` | `arm_down_L` | `wait` |
| `play` | `arm_down_L` | `wait` |
| `sad1` | `arm_down` | `wait` |
| `shy1` | `arm_down2` | `wait` |
| `sleep` | `arm_down_L` | `wait` |
| `smile1` | `arm_down_L` | `wait` |
| `surp1` | `arm_down_R` | `wait` |
| `think` | `arm_down_L` | `wait` |
| `touch` | `arm_down2` | `wait` |

`yes` 和 `no` 主要控制局部骨骼，因此作为覆盖动画播放；`skill1` 没有 `relay` 声明，被明确标注为独立动画。没有证据证明 `anger1 → anger2` 等同名编号动画一定需要串联，所以本阶段没有擅自添加这些推测组合。

## 七、表情覆盖与 Spine 多轨道

模型提供了 `face_wait`、`face_smile` 等一系列局部表情动画。当前使用三层轨道：

```text
Track 0 → 身体主动画、relay、wait
Track 1 → yes / no 等局部动作
Track 2 → 用户主动选择的 face_* 表情覆盖
```

工作台默认选项为“跟随原动画（推荐）”，此时不向 Track 2 写入任何动画，身体动作自带的表情完整保留。

只有用户主动选择 `face_smile`、`face_shy`、`face_serious` 等表情时，才在 Track 2 临时覆盖。进入复位阶段后调用：

```ts
avatar.state.setEmptyAnimation(2, 0.18)
```

让上层表情淡出并重新露出主动画或 `wait` 的原始表情。这样既能测试组合，又不会把人为选择误当成作者预设。

## 八、连续切换为什么会导致骨骼错位

最初的播放器每次收到新动作都会直接执行：

```ts
avatar.state.clearTracks()
```

`clearTracks()` 会清除正在播放和排队的 TrackEntry，但不等于让所有骨骼、插槽与附件瞬间恢复正确姿势。如果 `anger1` 播放到右手抬起时被清除，作者设计的 `arm_down_R` 就不会执行；而下一动画又可能没有为右手的每个属性写关键帧，于是右手保留在中间姿势并发生错位。

这类问题不限于手臂，也可能影响：

- 骨骼旋转和位移；
- 插槽附件显示状态；
- 表情与嘴型；
- 裁剪附件；
- 多轨道混合结果。

直接调用 `skeleton.setToSetupPose()` 虽然能清理残留，却会造成明显跳帧，也会绕过作者提供的收手动画，因此不作为正常恢复方案。

## 九、可中断动画状态机

### 1. 状态定义

播放器现在维护四个阶段：

```ts
type PlaybackPhase = 'idle' | 'intro' | 'loop' | 'recovering'
```

并区分：

```text
currentPlayback → 当前动作
pendingPlayback → 等待播放的最后一个动作
```

### 2. 混合中断规则

```text
idle 收到动作
→ 立即开始

intro 收到动作
→ 不在任意关键帧硬切
→ 等到 loop_start 边界
→ 执行当前动作 relay

loop 收到动作
→ 完成当前循环片段
→ 放弃尚未开始的重复片段
→ 执行当前动作 relay

recovering 收到动作
→ 不打断 relay
→ 只替换 pendingPlayback

relay 完成
→ 播放 pendingPlayback
→ 没有等待动作则进入 wait
```

例如在 `anger1` 尚未结束时请求 `touch`：

```text
anger1 intro / 当前循环片段
→ arm_down_R
→ touch intro
→ touch 循环段
→ arm_down2
→ wait
```

### 3. 只保留最后一次输入

连续输入不会无限调用 `addAnimation()`：

```text
hello → sad1 → shy1
```

如果当前动作仍在播放，等待槽会依次被覆盖，最后只保留 `shy1`。这能防止长动画排队数十秒，也符合桌宠交互中“用户最新意图优先”的原则。

### 4. 过期回调保护

播放器使用递增的 `playbackGeneration` 标记每次有效播放流程。旧 TrackEntry 即使在切换边界附近触发回调，也必须先核对 generation；不匹配的回调不会继续修改当前状态。

这解决了异步动画回调常见的竞争问题：旧动画的 `onComplete` 不应清除新动画的表情或错误启动另一个动作。

### 5. 没有 relay 时的兜底

如果动作没有声明 `relay`，或声明的动画在模型中不存在，播放器会混合回 `wait`，等待约 `220ms` 后再开始排队动作。这个兜底不会冒充作者设计，只负责保证控制器不会永远卡在 `recovering`。

## 十、验证方式与结果

本阶段执行：

```powershell
npm run build
```

验证结果：

1. Vue TypeScript 类型检查通过；
2. Vite 生产构建通过；
3. 所有明确包含 `relay` 的作者动作都已加入工作台；
4. 表情默认跟随原动画，覆盖项只在动作期间生效；
5. 连续动作请求只保留最后一个等待项；
6. 作者复位动画不会被下一次点击直接跳过；
7. 缺失 relay 时存在通用恢复路径；
8. 旧动画预设 ID 可以平滑迁移。

构建期间仍会看到 PixiJS 4 对 Node `path`、`url` 模块的浏览器外部化警告。这来自旧版 PixiJS 的依赖结构，目前没有阻止构建或已使用的渲染功能。

## 十一、当前不足与后续方向

### 1. 需要进行动画组合回归测试

状态机已经覆盖主要切换路径，但仍应逐组测试动作之间的衔接，特别是左右手姿势差异明显的组合：

```text
anger1 → touch
hello → sad1
play → sleep
shy1 → surp1
touch → think
```

### 2. 状态暂未显示在调试界面

工作台目前没有显示 `idle / intro / loop / recovering` 和等待动作名称。增加只读状态区域会更方便验证连续点击是否按照预期进入复位阶段。

### 3. 点击命中仍基于默认姿势

动作伸出的手臂可能不在默认 Alpha 蒙版内。动态命中需要在准确性、性能和窗口穿透稳定性之间继续权衡。

### 4. 动画优先级尚未产品化

当前只有“最后输入优先”。后续若加入睡眠、系统提醒、台词、随机待机和用户点击，需要给请求增加来源、优先级、可打断性和冷却时间。

### 5. 运行时仍属于旧版技术栈

PixiJS 4 与 pixi-spine 1.x 能较低成本兼容当前 Spine 3.6 模型，但依赖较旧。未来升级应先建立截图对比和动画回归用例，避免再次出现裁剪、附件或混合差异。

## 本阶段值得记忆的知识点

- Tauri 2 的前端 Window API 需要对应 Capability 权限；
- 透明窗口穿透必须把浮层控件加入交互白名单；
- WebGL Canvas 可以通过 Pixi extract 读取快照，但高频 GPU 回读成本较高；
- Spine 的绘制顺序和裁剪发生在 Canvas 内，CSS `z-index` 无法修复模型内部图层；
- `loop_start` 和 `relay` 是模型作者留给播放器的行为语义；
- Spine 多轨道适合把身体动作、局部动作和表情覆盖分开；
- 默认不写入覆盖轨道，是尊重原动画设计最可靠的方式；
- `clearTracks()` 不等于恢复完整骨骼姿势；
- 动画中断应选择安全边界，并完整执行复位动作；
- 单槽 pending queue 可以实现“最后输入优先”并防止队列膨胀；
- 异步回调需要 generation/token 防止旧流程污染新状态；
- 缺少作者元数据时可以提供通用兜底，但必须与原生预设明确区分。
