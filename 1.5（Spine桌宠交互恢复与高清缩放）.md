# 1.5 Spine 桌宠交互恢复与高清缩放

## 本阶段目标

1. 将独立验证完成的 `SpineAvatar.vue` 重新接入原有桌宠外壳；
2. 恢复窗口拖动、右键菜单、位置重置、退出和透明区域穿透；
3. 删除静态图片阶段的点击跳跃功能，为后续点击播放 Spine 动画留出空间；
4. 改善 2K 屏幕下模型偏小、边缘偏模糊的问题；
5. 让“调整大小”同时改变角色、Pixi 逻辑画布和 Canvas 像素尺寸。

本阶段延续 1.4 的职责划分：

```text
PetAvatar.vue
├── Tauri 窗口交互
├── 鼠标命中与穿透
├── 右键菜单
├── 缩放编辑与持久化
└── SpineAvatar.vue
    ├── PixiJS Application
    ├── Spine 模型加载
    ├── 动画播放
    └── 渲染尺寸与模型布局
```

## 一、把 Spine 放回桌宠交互层

1.4 阶段由 `App.vue` 直接挂载 `SpineAvatar`，目的只是隔离验证资源和旧版 Runtime。本阶段改回由 `App.vue` 挂载 `PetAvatar`，再由 `PetAvatar` 包含 `SpineAvatar`。

这样做的原因是渲染与桌面窗口行为属于不同职责：

- `SpineAvatar` 不需要知道右键菜单或 Tauri 窗口如何移动；
- `PetAvatar` 不直接操作骨骼、纹理和 WebGL；
- 以后替换角色或增加动画时，不必重写窗口交互代码。

Tauri 窗口高度由 `600` 增加到 `800`，主界面改为底部居中，让更大的全身角色仍能在透明窗口中完整显示。

## 二、恢复拖动、菜单与透明穿透

Spine 的最终输出是 `<canvas>`，原有交互不能继续直接绑定到静态 `<img>`。本阶段把以下事件绑定到新的 `.pet-avatar__body` 容器：

```text
pointerdown  → 记录按下位置
pointermove  → 超过阈值后调用 Tauri startDragging()
pointerup    → 释放指针捕获
contextmenu  → 打开自定义右键菜单
```

三横线拖动柄、调整大小、重置位置和退出功能继续保留。

浏览器预览环境不存在 Tauri 原生窗口对象，所以窗口对象改为按运行环境创建：

```ts
const appWindow = isTauri() ? getCurrentWindow() : null
```

这让组件既可以在 `npm run dev` 的浏览器中检查渲染，也可以在 `npm run tauri dev` 中调用窗口 API。

### 当前透明穿透方案

Canvas 每一帧都可能变化，直接高频读取 WebGL 像素代价较高。因此当前仍保留静态透明 PNG 作为不可见的命中蒙版：

```text
Spine Canvas  → 用户看到的动态角色
透明 PNG      → opacity: 0，仅提供 Alpha 命中数据
```

指针检测继续根据蒙版 Alpha 值决定是否调用：

```ts
appWindow.setIgnoreCursorEvents(true | false)
```

调整大小期间会强制保持窗口可交互，避免鼠标移动到边框或透明区域后突然失去拖动事件。

## 三、删除旧的跳跃功能

静态图片阶段的跳跃通过 CSS `@keyframes` 和 `transform` 完成。接入 Spine 后继续使用这套方案会产生两个问题：

1. CSS 移动的是整个 Canvas，而不是播放模型骨骼动画；
2. 点击、拖动阈值和 Spine 交互动画容易互相冲突。

因此本阶段删除了：

- `isJumping`；
- `isJumpEnabled`；
- 跳跃开关的本地存储；
- 右键菜单中的“禁用/启用跳跃”；
- `pet-jump` CSS 动画。

后续点击反馈应调用 `SpineAvatar` 暴露的 `playAnimation()`，播放 `touch`、`hello` 等模型自带动画，播放完再回到 `wait`。

## 四、清晰度问题的诊断过程

Tauri DevTools 中测得初始数据：

```text
window.devicePixelRatio = 1
Canvas 内部像素       = 384 × 544
Canvas 显示尺寸       ≈ 377.6 × 534.9
像素/界面尺寸比例     ≈ 1.02
```

这说明 Canvas 没有被严重地 CSS 拉伸，但只进行了接近 1× 的绘制。在 2K 屏幕上，人物眼睛、头发和衣服轮廓可用的采样像素较少，再叠加 Atlas 的线性过滤，视觉上会偏软。

网站预览和 Tauri 截图还存在“角色实际显示尺寸不同”的问题：网站中的角色更大、更接近特写，局部自然获得更多屏幕像素，不能只根据截图直接判定为素材质量不同。

## 五、2× 超采样方案

渲染分辨率现在限制在 `2×～3×`：

```ts
const minimumResolution = 2
const maximumResolution = 3
```

实际倍率由系统 DPR 决定，但不会低于 2，也不会高于 3：

```ts
Math.min(
  maximumResolution,
  Math.max(minimumResolution, window.devicePixelRatio || 1),
)
```

例如角色显示尺寸为 `384 × 544`、渲染倍率为 2 时：

```text
逻辑显示尺寸：384 × 544
Canvas 像素： 768 × 1088
```

这不会改变角色在屏幕上的大小，只会让同一显示面积拥有更多绘制像素。实际观察中，模型头发、眼睛和服装边缘明显更清晰。

最高限制为 3 是性能保护。Canvas 尺寸翻倍后，像素数量会变成四倍；倍率继续增加会提高显存占用和每帧填充成本，但画质收益会逐渐降低。

## 六、第一次放大方案为什么失败

最初将缩放值同时用于：

```text
外层容器尺寸
Canvas resolution
```

调整框可以从 `384 × 544` 变为约 `538 × 762`，百分比也能达到 `140%`，但截图显示角色仍停留在旧画布范围内，并且偏在放大框左侧。

根本原因是混淆了两种不同的“缩放”：

```text
显示缩放（display size） → 决定角色在屏幕上有多大
渲染倍率（resolution）   → 决定相同显示尺寸使用多少像素
```

当 `renderScale` 只提高 Pixi Renderer 的 `resolution` 时，画面会更清晰，却不会自动让 Spine 模型变大。外层容器虽然改变了尺寸，但依赖 `ResizeObserver` 间接传播尺寸在实际 Tauri 环境中没有可靠地完成 Pixi 逻辑尺寸更新。

因此出现了：

```text
petScale = 140%
外层调整框变大
Pixi 逻辑画布仍接近原尺寸
Spine 模型没有同步放大
```

## 七、最终的真实缩放链路

最终方案把显示尺寸和渲染倍率彻底分离。

`PetAvatar.vue` 根据缩放比例计算明确尺寸：

```ts
const petDisplayWidth = computed(() => 384 * petScale.value)
const petDisplayHeight = computed(() => 544 * petScale.value)
```

随后把数值作为 Props 传入渲染组件：

```vue
<SpineAvatar
  animation="wait"
  :display-width="petDisplayWidth"
  :display-height="petDisplayHeight"
/>
```

`SpineAvatar.vue` 监听两个显示尺寸，在 Vue 完成 DOM 更新后安排下一帧重排：

```text
用户拖动缩放柄
→ petScale 改变
→ computed 重新计算宽高
→ displayWidth / displayHeight Props 更新
→ requestAnimationFrame
→ renderer.resize(width, height)
→ 重新计算 Spine bounds、scale 和 position
```

在 `140%` 时：

```text
显示宽度：384 × 1.4 = 537.6
显示高度：544 × 1.4 = 761.6
2× Canvas：约 1076 × 1524
```

因此调整框、Canvas 和 Spine 角色会同步放大，同时保持 2× 清晰度。

## 八、缩放交互细节

当前缩放范围：

```text
最小：60%
默认：100%
最大：140%
```

右下角增加实时百分比，便于判断是否已经达到上限。缩放开始时固定记录起始比例，并使用固定基准宽度计算增量：

```ts
petScale = startScale + horizontalDistance / 384
```

使用固定基准值可以避免“元素放大后分母也变大”，否则拖动过程中可能出现比例回缩、变化不线性或手柄抖动。

确认按钮会把比例写入：

```text
localStorage['mmm-pet-scale']
```

重置按钮恢复为 `100%`，按 `Escape` 则撤销本次调整，回到上一次确认的比例。

## 九、为什么当前最大为 140%

当前 Tauri 透明窗口是 `800 × 800`，底部还保留约 `16px` 间距。角色在 140% 时高度约为 `762px`，已经接近窗口可用高度。

如果直接允许 180%：

```text
544 × 1.8 ≈ 979px
```

角色会超过当前窗口并被裁切。后续若需要更大范围，必须同步扩大 Tauri 窗口，或实现根据缩放比例动态调整原生窗口尺寸。

## 十、验证结果

本阶段完成以下验证：

1. `npm run build` 通过 Vue TypeScript 检查与 Vite 生产构建；
2. Spine `wait` 动画可以在透明无边框 Tauri 窗口中运行；
3. 2× 超采样后清晰度明显改善；
4. 调整框、Pixi 画布和角色可以从 60% 到 140% 同步缩放；
5. 缩放过程中百分比实时更新，确认后能够持久化；
6. 窗口拖动、菜单、重置位置、退出和透明穿透继续保留。

构建仍会报告 PixiJS 4 对 Node `path`、`url` 的浏览器外部化警告。这是旧版依赖的技术债务，目前不影响已使用的 Spine 渲染链路，但发布安装包前仍需进行完整回归测试。

## 十一、当前不足与后续方向

### 1. 命中蒙版与动画姿势不完全一致

透明穿透仍使用静态 PNG。角色播放幅度较大的 Spine 动画后，真实身体位置可能超出或离开静态蒙版。长期方案可以使用骨骼包围盒、简化多边形命中区，或低频读取渲染结果构建动态蒙版。

### 2. 缩放上限受窗口尺寸限制

当前窗口固定为 `800 × 800`。更完善的产品应在确认缩放后同步调整原生窗口尺寸，并修正窗口位置，避免角色超出屏幕工作区。

### 3. 尚未接入交互动画状态机

点击跳跃已经删除，但还没有接入 `touch → wait` 等动画切换。后续应处理动画完成事件、重复点击、动画优先级和轨道混合。

### 4. 性能需要实机观察

2× Canvas 的像素数量是 1× 的四倍。后续应在任务管理器和 DevTools 中观察 GPU、内存、帧率及多显示器切换后的 DPR 变化。

## 本阶段值得记忆的知识点

- Canvas 的 CSS 显示尺寸和内部像素尺寸是两个概念；
- `devicePixelRatio` 描述设备像素与 CSS 像素的关系；
- 提高 `resolution` 能改善清晰度，但不会自动放大角色；
- 真正的缩放必须同步改变逻辑画布尺寸和模型布局；
- `computed` 适合从一个比例派生宽度、高度和百分比；
- Vue 更新 Props 后，可通过 `requestAnimationFrame` 安排第三方渲染器重排；
- `ResizeObserver` 很有用，但关键状态最好通过明确 Props 传递，而不是只依赖隐式 DOM 观察；
- 缩放计算应固定手势开始时的基准，避免反馈值反过来影响计算公式；
- 超采样倍率翻倍时，像素数量约变为四倍，需要设置合理上限；
- 动态画面与静态 Alpha 蒙版可能逐渐不一致，这是后续需要解决的交互技术债务。
