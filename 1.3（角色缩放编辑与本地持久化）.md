# 1.3 角色缩放、交互偏好与窗口控制

## 本次目标

为静态 PNG 桌面宠物完善右键菜单中的三项控制：调整大小、禁用跳跃和重置位置。

- 用户可以进入缩放编辑模式，拖动选中方框右下角调整角色大小，使用 `✓` 确认保存，使用 `↺` 恢复默认大小。
- 用户可以切换跳跃是否启用；菜单文案会随状态变为“禁用跳跃”或“启用跳跃”。
- 用户可以将被拖动过的原生窗口重新置于屏幕中央。

本版本只缩放角色画面，不改变 Tauri 原生窗口尺寸。

## 一、交互流程

```text
右键角色 → 调整大小
        ↓
出现角色选中方框与右下角控制区
        ↓
拖动小方块 → 实时预览缩放
        ↓
↺ 恢复 100% → 继续预览或按 ✓ 保存
        ↓
✓ 确认 → 保存比例并退出编辑模式

Esc → 放弃本次未确认的预览，恢复上次已保存比例
```

缩放范围固定为 `60% ~ 140%`，默认大小为 `100%`。限制范围能避免角色缩得太小而难以交互，也避免放大后超出固定窗口的可视范围。

## 二、缩放状态：预览值与已确认值

组件维护两个缩放状态：

```ts
const petScale = ref(1)
const confirmedPetScale = ref(1)
```

- `petScale`：当前正在画面上预览的比例。
- `confirmedPetScale`：用户最后一次点击 `✓` 保存的比例。

拖动时只改动 `petScale`。点击 `✓` 后，才把当前比例复制给 `confirmedPetScale` 并写入本地存储。按 `Esc` 时将 `petScale` 恢复为 `confirmedPetScale`，实现“取消未保存预览”。

## 三、为什么缩放角色而不是缩放窗口

Tauri 窗口目前固定为 `800 × 600`。第一版只修改角色按钮的 CSS 变换：

```css
transform: scale(var(--pet-scale));
transform-origin: bottom center;
```

`transform-origin: bottom center` 的含义是以角色底部中心作为锚点。角色放大或缩小时，脚底大致留在原地，不会像从中心缩放一样上下漂移。

如果改为缩放原生窗口，还需要调用 Tauri 的窗口尺寸 API、增加额外 capability 权限，并重新处理窗口边缘、透明区域、不同显示器 DPI 与内容裁切问题。因此先将“角色缩放”和“窗口大小”分开。

## 四、拖动右下角的缩放计算

右下角拖动点使用 `PointerEvent`：

```text
按下拖动点 → 记录起始鼠标坐标与起始缩放值
        ↓
指针移动 → 计算横向与纵向的平均位移
        ↓
位移 ÷ 图片基础宽度 → 得到缩放增量
        ↓
限制在 0.6 ~ 1.4 → 更新预览
```

核心计算如下：

```ts
const distance = (deltaX + deltaY) / 2
const nextScale = resizeStartScale + distance / baseWidth
petScale.value = clampPetScale(nextScale)
```

向右下拖动时，`distance` 为正，角色变大；向左上拖动时为负，角色变小。`setPointerCapture()` 保证鼠标离开小拖动点后，拖动事件仍由该按钮继续接收。

## 五、选中方框如何跟随角色

角色会被 CSS 缩放，不能直接用固定宽高绘制选中方框。代码读取图片实际渲染后的 `getBoundingClientRect()`，再换算为相对宠物组件的坐标：

```ts
const imageRect = image.getBoundingClientRect()
const avatarRect = avatar.getBoundingClientRect()

left = imageRect.left - avatarRect.left
top = imageRect.top - avatarRect.top
width = imageRect.width
height = imageRect.height
```

因此缩放预览时，虚线方框会跟随图片当前的屏幕尺寸更新。

## 六、缩放与跳跃动画共存

缩放和跳跃都涉及 CSS `transform`。如果同时把两种变换写在同一个元素上，动画会覆盖缩放值。

本项目将两层变换分开：

- 外层角色按钮负责 `scale(var(--pet-scale))`。
- 内层 `<img>` 在跳跃时执行上下位移和轻微弹性缩放动画。

这样角色无论被调成多大，跳跃动画都能正常播放；进入调整大小模式后，角色的点击跳跃和拖动窗口手势会暂时停用，避免编辑手势冲突。

## 七、透明区域穿透下的编辑控件

桌面宠物会对 PNG 透明像素启用鼠标穿透。调整大小模式中，右下角拖动点、`↺` 和 `✓` 位于角色图像之外，如果不额外处理，鼠标会直接穿透到桌面。

为此，鼠标命中判断额外检查三个控件的实际矩形：

```text
角色不透明像素 → 接收事件
拖动点 / ↺ / ✓ → 接收事件
其他透明区域 → 穿透到桌面
```

选中方框自身只有视觉意义，因此设置 `pointer-events: none`，不会阻挡框内原本应当穿透的透明区域。

## 八、本地保存

确认缩放时会写入浏览器存储：

```ts
localStorage.setItem('mmm-pet-scale', String(petScale.value))
```

下次启动 Tauri 应用时，组件会读取该值并恢复已保存的比例。`localStorage` 适合保存这类小型界面偏好；未来如果需要管理多个宠物、预设方案或复杂配置，再考虑 Tauri Store。

## 九、禁用跳跃：状态与菜单文案联动

跳跃开关使用 `isJumpEnabled` 表示：

```ts
const isJumpEnabled = ref(true)
```

菜单不会显示一个不可点击的“已禁用”标签，而是显示下一步可执行的操作：

```text
跳跃已启用 → 菜单显示“禁用跳跃”
跳跃已禁用 → 菜单显示“启用跳跃”
```

点击该项时，`toggleJump()` 会反转布尔值，并保存到 `localStorage`：

```ts
localStorage.setItem('mmm-pet-jump-enabled', String(isJumpEnabled.value))
```

`jumpPet()` 的第一步会检查这个状态；若跳跃已禁用，函数直接结束，不会添加跳跃动画 class。角色的拖动功能仍然保留，因此“禁用跳跃”不会影响移动窗口。

## 十、重置位置：调用 Tauri 原生窗口能力

角色窗口可被拖动到桌面任意位置。菜单中的“重置位置”调用：

```ts
await getCurrentWindow().center()
```

`center()` 由 Tauri 的原生窗口层执行，负责将窗口移动到当前屏幕中央。它不是 CSS 位移，也不会改变角色自身的缩放比例。

Tauri 2 默认不会让网页随意移动原生窗口，因此 capability 配置需要明确授权：

```json
"core:window:allow-center"
```

修改 capability 后必须重新启动 `npx tauri dev`，仅依赖 Vite 热更新无法加载新的原生权限。

## 十一、当前菜单功能边界

```text
禁用跳跃 / 启用跳跃 → 切换并持久化交互偏好
调整大小             → 进入可确认、可取消的缩放编辑模式
重置位置             → 窗口回到屏幕中央
退出                 → 关闭 Tauri 原生窗口
```

菜单目前没有二级菜单、快捷键提示或“恢复所有默认设置”。这些可在后续将多个偏好组合为“宠物设置”面板时统一完善。

## 相关文件

- `src/components/PetAvatar.vue`：缩放编辑、跳跃开关、窗口居中、选中方框、控制按钮和本地存储。
- `src-tauri/capabilities/default.json`：`allow-center`、`allow-close`、`allow-start-dragging` 等原生窗口能力授权。
