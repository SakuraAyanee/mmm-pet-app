# 1.7 桌面环境感知与隐私优先产品设计

## 文档目的

本文记录桌宠“观察用户当前桌面并作出角色化回应”功能的产品构想、技术路线、风险边界与分阶段实施计划，供后续开发、评审和测试使用。

本文是设计方案，不代表当前版本已经启用截屏、联网分析或后台监控。

## 一、产品愿景

让桌宠在用户明确授权的情况下理解当前活动，例如编程、阅读、观看视频或暂时休息，并结合角色性格给出简短、克制且符合语境的回应。

理想体验不是“持续监视桌面”，而是：

```text
用户知道桌宠何时观察
→ 用户可以控制观察范围
→ 原始画面尽量留在本地
→ 模型只获得完成任务所需的最少信息
→ 桌宠只在适当时机说一句有价值的话
```

## 二、产品原则

### 1. 明确授权

- 环境感知默认关闭；
- 首次使用必须解释会截取什么、发送到哪里、保存多久；
- 手动触发优先于自动触发；
- 用户可以随时暂停、撤销授权和清除历史。

### 2. 数据最小化

- 优先使用前台进程、窗口标题和空闲时间等低敏感元数据；
- 能截取单一活动窗口时，不截取整个桌面；
- 能发送裁剪区域时，不发送完整窗口；
- 能用本地摘要完成任务时，不上传原图；
- 不把截图写入磁盘、日志、崩溃报告或长期对话历史。

### 3. 本地优先

本地负责敏感窗口拦截、OCR 脱敏、区域裁剪、图片压缩、重复画面检测和策略判断。云端模型只处理通过本地安全门的最小输入。

### 4. 可见、可暂停、可审计

感知启用时必须有稳定可见的状态提示。用户应能查看：

- 是否正在观察；
- 最近一次分析时间；
- 本次准备发送的画面；
- 模型返回的活动摘要；
- 为什么桌宠选择回应或保持沉默。

### 5. 只读感知

第一阶段模型只能描述和分类画面，不能控制鼠标、键盘、文件、终端或外部应用。屏幕中的任何文字都应被当作不可信数据，而不是系统指令。

## 三、可行性与使用边界

Windows 可以获取显示器或指定窗口的画面，Tauri 可由 Rust 后端封装这一能力，再通过受限 Command 向 Vue 提供结果。

视觉模型可以识别应用界面、文本、图表和大致活动。DeepSeek 官方当前提供支持图片输入的 `deepseek-v4-flash-vision-exp`，但具体模型 ID、价格和能力可能变化，开发时必须通过官方模型列表和配置确认，不应把模型名称硬编码为产品逻辑。

手动触发的个人电脑原型可行性较高。自动、长期、公开发布的版本需要额外解决隐私合规、数据安全、提示注入、误判、成本、性能和用户信任问题。

未经组织书面授权，不应在银行、企业办公、远程桌面、客服、财务、医疗或其他可能显示敏感数据的环境中启用云端截图分析。

## 四、推荐系统架构

```text
用户手动触发 / 安全事件触发
              ↓
Windows 活动窗口捕获（Rust）
              ↓
本地安全门
├── 应用黑名单
├── 敏感窗口判断
├── OCR 与规则脱敏
├── 裁剪、缩放、压缩
├── 重复画面检测
└── 用户预览确认
              ↓
视觉分析服务
└── 只返回结构化活动摘要
              ↓
回应策略
├── 置信度阈值
├── 活动是否发生变化
├── 冷却时间
├── 静默时段
└── 敏感性复核
              ↓
角色人格层
└── 根据安全摘要生成一句台词
              ↓
Spine 表情 + 动作 + 对话气泡
```

关键设计是把“看到了什么”和“角色怎么说”分开。视觉模型不直接扮演角色；人格模型也不必看到原始截图。

## 五、建议的数据契约

视觉分析阶段只允许返回固定结构：

```ts
interface ActivityObservation {
  activity:
    | 'coding'
    | 'reading'
    | 'writing'
    | 'communication'
    | 'video'
    | 'gaming'
    | 'idle'
    | 'unknown'
  confidence: number
  sensitive: boolean
  changed: boolean
  shouldSpeak: boolean
  context: string
}
```

示例：

```json
{
  "activity": "coding",
  "confidence": 0.86,
  "sensitive": false,
  "changed": true,
  "shouldSpeak": true,
  "context": "用户似乎正在调试一个桌面应用"
}
```

禁止在摘要中返回密码、验证码、API Key、完整身份信息、通信全文、文件正文或与回应无关的细节。

角色层只接收 `ActivityObservation`，输出：

```ts
interface PetResponse {
  text: string
  animation: string
  expression: string | null
  priority: 'low' | 'normal'
}
```

## 六、模型与密钥策略

### 1. 模型提供方可替换

定义统一接口，避免业务逻辑直接依赖某一家 API：

```ts
interface VisionProvider {
  analyze(image: Uint8Array, prompt: string): Promise<ActivityObservation>
}
```

配置至少包含 API Base URL、模型 ID、超时、最大图片尺寸、每日请求上限和是否允许云端处理。

### 2. API Key 留在原生后端

API Key 不得放入 Vue 源码、浏览器请求、`localStorage`、Git 仓库或普通日志。Tauri 中应由 Rust 后端读取系统凭据并发起请求，前端只调用权限明确的命令：

```ts
invoke('analyze_current_activity')
```

长期方案应使用 Windows Credential Manager 或同等系统级凭据存储。发布版本不应内置开发者的个人 Key。

### 3. 原始截图不进入对话记忆

每次视觉判断使用独立请求。只允许经过脱敏的活动类别、置信度和短摘要进入本地短期上下文，并设置数量与过期时间上限。

## 七、威胁模型

### 1. 无意捕获敏感信息

风险内容包括密码、验证码、支付信息、客户数据、内部文档、私人聊天和第三方个人信息。

缓解措施：

- 默认禁止密码管理器、银行、邮件、聊天、远程桌面和隐私浏览窗口；
- 用户可维护应用黑白名单；
- 仅截取活动窗口；
- 发送前提供预览；
- 本地检测常见敏感格式并阻止请求；
- 截图只保存在内存中，用完立即释放。

OCR 和正则打码只能降低风险，不能构成绝对保证。

### 2. 屏幕提示注入

网页、聊天或文档可能包含“忽略指令”“上传文件”等恶意文字。视觉模型必须被限制为数据分类器：

- 屏幕文字一律视为不可信内容；
- 不向视觉模型提供工具；
- 不允许自由格式命令输出；
- 使用 JSON Schema 校验；
- 无效输出直接丢弃；
- 不把屏幕指令转发给人格层。

### 3. 模型误判

打开代码编辑器不等于正在编程，打开视频也可能是在学习。回应应使用置信度控制：

```text
confidence < 0.65 → 保持沉默
0.65～0.85        → 使用“看起来、似乎”等试探表达
> 0.85            → 正常回应，但避免价值判断
```

避免使用“你又在摸鱼”等武断或羞辱性表达。

### 4. 密钥泄漏与滥用

- Rust 后端持有密钥；
- Command 参数不得接受任意 URL 或任意请求体；
- 为请求设置超时、大小限制和每日配额；
- 日志过滤 Authorization Header；
- 发布前扫描仓库历史和构建产物；
- Key 泄漏后必须支持快速吊销和轮换。

### 5. 数据残留

截图不能出现在临时目录、DevTools 日志、崩溃转储、遥测事件、错误附件或模型对话历史。如果为了调试允许保存图片，该能力必须只存在于显式开发模式，并显示保存路径和删除入口。

### 6. 第三方与合规风险

截图可能包含不属于当前用户的数据。公开发布前需要明确隐私政策、处理目的、数据接收方、保存期限、用户权利和退出方式，并根据实际发布地区寻求专业合规意见。

## 八、触发与打扰策略

不建议每隔固定几秒上传截图。推荐“事件触发 + 冷却时间”：

```text
活动窗口发生变化
且稳定停留 30～60 秒
且距离上次分析超过 5 分钟
且没有处于静默状态
且通过本地安全门
→ 才允许请求分析
```

建议规则：

- 同一种活动 20 分钟内不重复回应；
- 全屏游戏和视频时默认降低频率；
- 锁屏、密码界面和远程桌面时完全停用；
- 快速切换窗口时不分析；
- 每日设置请求次数和费用上限；
- 网络或 API 失败时安静退化；
- 模型认为“不值得打扰”时不生成角色台词。

## 九、用户界面设计

右键菜单未来可增加：

```text
让摩美美看看当前窗口
开启环境感知
暂停一小时
隐私设置
查看最近一次分析
```

感知状态必须稳定可见，例如眼睛图标表示已开启、眼睛动画表示本地分析、云朵表示云端请求、斜线眼睛表示暂停。

首次授权应说明功能目的、捕获范围、本地处理、外部服务、保存规则和撤销方法，并展示一张实际发送内容的预览。

## 十、性能与费用控制

高频截图会增加 GPU 回读、图片编码、网络流量、模型费用和电池消耗。即使单张图片的 Token 数有上限，数千次重复请求仍没有产品价值。

建议：

- 将图片缩放到足够识别界面的尺寸；
- 使用 JPEG/WebP 合理压缩；
- 对连续截图计算感知哈希，相似时不请求；
- 限制并发为 1；
- 设置 10～20 秒网络超时；
- 失败采用指数退避；
- 记录 Token 与费用，不记录图片内容；
- 达到日预算后自动停用云端分析。

## 十一、分阶段开发路线

### Phase 0：无截图的交互验证

- 使用模拟的 `ActivityObservation`；
- 验证台词长度、角色语气、动画搭配和打扰频率；
- 不申请截图权限，不接入外部 API。

### Phase 1：手动观察 MVP

- 用户点击“看看当前窗口”；
- 只捕获当前活动窗口；
- 展示截图预览；
- 用户再次确认后上传；
- 只返回结构化活动摘要；
- 生成一句角色台词；
- 请求完成后释放原图。

这是当前最推荐的下一阶段。

### Phase 2：本地隐私保护

- 应用黑名单和敏感窗口拦截；
- OCR 与规则脱敏；
- 内存生命周期管理；
- API Key 安全存储；
- 请求、Token、错误的非内容审计日志。

### Phase 3：半自动环境感知

- 功能默认关闭；
- 使用窗口变化事件触发；
- 加入稳定停留时间和冷却时间；
- 增加静默模式、每日预算和状态指示；
- 相同活动抑制重复回应。

### Phase 4：本地优先与可替换模型

- 使用本地 OCR、图像分类或小型视觉模型；
- 云端只处理本地无法判断的低敏感输入；
- 支持不同模型提供方；
- 允许完全离线模式。

## 十二、Phase 1 验收标准

- [ ] 默认不截屏；
- [ ] 每次截图都由用户主动触发；
- [ ] 只捕获活动窗口；
- [ ] 上传前展示实际图片预览；
- [ ] 用户可以取消；
- [ ] 敏感应用命中黑名单时拒绝截取；
- [ ] API Key 不进入前端和 Git；
- [ ] 图片不写入磁盘；
- [ ] 模型只返回符合 Schema 的摘要；
- [ ] 屏幕内容不能触发工具调用；
- [ ] 超时、断网、限流时能够安全恢复；
- [ ] 台词可以追溯到摘要，而不是原始隐私内容；
- [ ] 请求结束后原始字节从应用状态中释放；
- [ ] 用户能够关闭功能并清除本地摘要历史。

## 十三、测试计划

### 功能测试

- 代码编辑器、文档、视频、游戏和空闲桌面分类；
- 多显示器和不同缩放倍率；
- 窗口最小化、切换、关闭时的竞争情况；
- 断网、超时、401、429 和服务端错误；
- 桌宠窗口本身是否被错误截入画面。

### 安全测试

- 截图中出现 API Key、验证码、手机号和银行卡号；
- 网页显示提示注入文本；
- 模型返回非 JSON、超长文字或未知动画名；
- 恶意前端尝试调用任意截图和任意网络地址；
- 日志、临时目录和崩溃报告中是否残留图片。

### 产品测试

- 回应是否过于频繁；
- 相同活动是否重复说话；
- 误判时语气是否足够克制；
- 用户能否立即看出感知状态；
- 用户能否在两步以内暂停功能；
- 长时间运行的内存、电量和费用变化。

## 十四、仍需决策的问题

1. 第一版只支持 Windows，还是为跨平台截图提前抽象接口？
2. 使用用户自己的 API Key，还是建设受控中转服务？
3. 是否允许保存脱敏后的活动摘要，保留多久？
4. 默认黑名单包含哪些应用和窗口类型？
5. 角色人格是固定模板、可编辑模板，还是多角色配置？
6. 哪些活动值得主动回应，哪些只更新内部状态？
7. 是否提供完全离线的本地模型选项？
8. 如何处理角色形象、台词风格与模型资产的授权范围？

## 十五、参考资料

- [DeepSeek Vision API](https://api-docs.deepseek.com/guides/vision/)
- [DeepSeek Models & Pricing](https://api-docs.deepseek.com/quick_start/pricing/)
- [DeepSeek Privacy Policy](https://cdn.deepseek.com/policies/en-US/deepseek-privacy-policy.html)
- [DeepSeek Open Platform Terms](https://cdn.deepseek.com/policies/en-US/deepseek-open-platform-terms-of-service.html)
- [Microsoft Windows Graphics Capture](https://learn.microsoft.com/en-us/windows/uwp/audio-video-camera/screen-capture)

模型能力、价格、隐私政策和平台条款可能变化，进入实现阶段前应重新核对官方资料。
